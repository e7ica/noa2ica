<!-- Modal de Confirmación de Sanción -->
<div id="confirmacionSancionModal" class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50">
    <div class="bg-white rounded-lg shadow-lg p-6 w-96 my-64 md:w-[550px]">
        <h2 class="text-lg font-semibold mb-4 flex items-center justify-between">
            <span>🚨</span>
            ¿Cuál será la sanción?
            <span>⚖️</span>
        </h2>

        <!-- Detalles de la sanción -->
        <div class="flex-1 overflow-y-auto overflow-x-hidden">

            <!-- Tipo de sanción -->
            <div class="flex hidden items-center space-x-2">
                <i class="fas fa-exclamation-triangle text-yellow-500"></i>
                <p class="text-sm text-gray-600">Tipo de sanción:
                    <span class="text-yellow-600 font-semibold">Multa económica</span>
                </p>
            </div>

            <!-- Monto de la sanción -->
            <div class="flex hidden items-center space-x-2">
                <i class="fas fa-dollar-sign text-green-500"></i>
                <p class="text-sm text-gray-600">Monto a pagar:
                    <span class="text-green-600 font-semibold">$5,000</span>
                </p>
            </div>

            <!-- Nota aclaratoria con lista de instrucciones -->
            <div class="mt-0 px-4 py-4 border border-red-300 bg-red-50 rounded-lg text-sm flex flex-col max-h-[100vw] sm:max-h-64 overflow-y-scroll overflow-x-hidden    ">
                <p class="hidden text-sm text-gray-700 font-semibold mb-2">Instrucciones:</p>
                <ul class="list-disc pl-2 text-gray-700 space-y-1">
                    <li>El pago de la multa debe realizarse dentro de los próximos <span class="text-red-500 font-semibold">15 días</span>.</li>
                    <li>La multa puede ser pagada en línea a través del portal municipal o en cualquier sucursal bancaria.</li>
                    <li>Una vez pagada, se debe enviar el comprobante de pago a la dirección de correo indicada.</li>
                    <li>Si no se cumple con el pago dentro del plazo, se aplicarán medidas adicionales como el cierre temporal del comercio.</li>
                    <li>El pago de la multa debe realizarse dentro de los próximos <span class="text-red-500 font-semibold">15 días</span>.</li>
                    <li>La multa puede ser pagada en línea a través del portal municipal o en cualquier sucursal bancaria.</li>
                    <li>Una vez pagada, se debe enviar el comprobante de pago a la dirección de correo indicada.</li>
                    <li>Si no se cumple con el pago dentro del plazo, se aplicarán medidas adicionales como el cierre temporal del comercio.</li>
                </ul>
            </div>
        </div>

        <!-- Botones de confirmación -->
        <div class="flex flex-col justify-center mt-4 space-y-4">
            <!-- Botón para confirmar la sanción -->
            <button id="confirmarSancionBtn" class="bg-red-500 px-6 py-3 hover:bg-red-600 text-white text-lg font-semibold rounded-full shadow-md flex items-center justify-center transition duration-300 ease-in-out">
                <i class="fas fa-check-circle mr-2"></i>
                Confirmar Sanción
            </button>

            {% if back %}
            <!-- Botón para volver atrás -->
                <button id="volverAtrasSancionBtn" class="px-6 py-3 text-gray-500 hover:text-gray-600 text-lg font-semibold rounded-full flex items-center justify-center transition duration-300 ease-in-out">
                    <i class="fas fa-arrow-left mr-2"></i>
                    No, Volver Atrás
                </button>
            {% else  %}
                <button id="cancelarSancionBtn" class="px-6 py-3 text-gray-500 hover:text-gray-600 text-lg font-semibold rounded-full flex items-center justify-center transition duration-300 ease-in-out">
                    No, Cancelar
                </button>
            {% endif %}
        </div>
    </div>
</div>


<script type="module">

    //  Controlar la confirmación o vuelta atrás en el modal de sanción
    //  si es cancelar, se cierra el modal
    document.getElementById('cancelarSancionBtn').addEventListener('click', function(e) { e.preventDefault();
        document.getElementById('confirmacionSancionModal').classList.add('hidden');
    });
</script>